# Embedded host — placeholder. Replace SDL2 with a thin HAL (framebuffer
# flush, I²S/PWM audio, GPIO buttons). Builds nothing yet.
message(STATUS "Cronopio embedded target: stub only — see host/embedded/README.md")
